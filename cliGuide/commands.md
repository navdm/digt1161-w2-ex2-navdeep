# Essential Git CLI Commands

## git clone
Creates a clone of another git repository.

## git checkout
Switches to the specified git branch, or if used with the -b flag, creates and switches to the branch.

## git commit
Saves the staged changes to the local repostory's history as a snapshot..